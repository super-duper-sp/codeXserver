# Node.js dependencies
/node_modules

# Build output
/build
/dist

# Dependency directories for IDEs
/.vscode
/.idea

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (Sensitive Information)
.env
.env.local
.env.*.local

# OS-specific files
.DS_Store       # macOS
Thumbs.db       # Windows

# Coverage directory for testing (e.g., Jest)
/coverage

# npm package lock (optional, if you use npm)
/package-lock.json

# Yarn files (if using Yarn instead of npm)
/yarn.lock

# React Native (if applicable)
.expo
.expo-shared

# TypeScript (if using TypeScript)
/*.tsbuildinfo

# Optional: Firebase, Netlify, etc.
firebase-debug.log
netlify.toml

# Ignore the test coverage report files generated by Jest
/coverage

# Ignore database files (if using SQLite, for example)
/*.sqlite
